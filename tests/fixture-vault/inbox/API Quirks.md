# API Quirks.md (Depth 1)

Documentation of API behavior, only under Documentation/ (no duplication).

**Expected location:** `Documentation/API Quirks.md`

## Known Behaviors

- Rate limiting is per-IP, not per-user
- Response times vary with payload size
